spring.application.name=spring-project
server.port=8080

# none: do nothing
# validate: only validate the schema, make no changes to the database
# update: update the schema if the table exists, DO NOT create new tables if they do not exist
# create: create the entire table and DELETE old data
# create-drop: same as create, but DROP the table when the application stops

# Remote database configuration
spring.datasource.url=jdbc:mysql://server.aptech.io:3307/fastroute_test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=fastroute_user
spring.datasource.password=fastroute_password

# Local MySQL80 database configuration
# spring.datasource.url=jdbc:mysql://localhost:3306/fastroute_test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=somethings

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Thêm vào application.properties để debug
logging.level.org.springframework.web=DEBUG
logging.level.ktc.spring_project.controllers=DEBUG
# logging.level.ktc.spring_project=DEBUG
# logging.level.org.springframework=DEBUG
# logging.level.org.hibernate=DEBUG
# spring.web.mvc.trailing-slash.matching=true
# logging.level.org.springframework.security=DEBUG

# JWT Configuration
# jwt.secret=this-is-a-very-secure-32-byte-secret!

jwt.secret=9a0a65be0f6447c28e4bb9e30d5354f7aafde3e62778ed0c0c6a09b59bba2a30
# JWT token expiration: 15 minutes = 900000 milliseconds (15 * 60 * 1000)
jwt.expiration=900000
google.maps.api.key=AIzaSyAoFD64b2leogcIyl3BgKjdWSvFT5PaLx0

# ============ EMAIL CONFIGURATION cho INVOICE SYSTEM ============
# SMTP Configuration (sử dụng Gmail làm ví dụ)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:datkz31@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:isrboiphvwigcvci}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Invoice Email Settings - Cấu hình riêng cho hóa đơn
invoice.email.enabled=${INVOICE_EMAIL_ENABLED:true}
invoice.email.from=${INVOICE_EMAIL_FROM:noreply@ktc-logistics.com}
invoice.email.subject.prefix=KTC Logistics - 
invoice.email.auto.send=${INVOICE_AUTO_SEND:true}

# Company Information - Thông tin công ty hiển thị trong email
company.name=KTC Logistics
company.phone=+84-123-456-789
company.email=support@ktc-logistics.com
company.website=https://ktc-logistics.com
company.address=Phi Binh, Ngu Hanh Son, Da Nang

# File Storage - Đường dẫn lưu trữ file PDF
# Sử dụng thư mục trong project cho development, production có thể override
invoice.pdf.storage.path=${INVOICE_PDF_PATH:./invoices/pdfs}

# Invoice Business Rules - Quy định nghiệp vụ hóa đơn
# Thời gian tối đa để xuất hóa đơn sau khi giao hàng (tính bằng ngày)
# Mặc định: 180 ngày (6 tháng) - có thể override qua biến môi trường
invoice.expiry.days=${INVOICE_EXPIRY_DAYS:365}

# Previous local configuration (removed)

# ============ SWAGGER UI CONFIGURATION ============
# Đường dẫn Swagger UI - đặt mặc định
springdoc.swagger-ui.path=/swagger-ui.html
# Bật tính năng Swagger UI
springdoc.swagger-ui.enabled=true
# Sắp xếp các operations theo phương thức HTTP
springdoc.swagger-ui.operationsSorter=method
# Sắp xếp các tag theo thứ tự alphabet
springdoc.swagger-ui.tagsSorter=alpha
# Mặc định đóng tất cả các mục khi mở Swagger UI
springdoc.swagger-ui.docExpansion=none
# Cho phép tính năng filter
springdoc.swagger-ui.filter=true
# Hiển thị thẻ Request Duration
springdoc.swagger-ui.displayRequestDuration=true
# Cho phép tính năng tryItOut mặc định
springdoc.swagger-ui.tryItOutEnabled=true
# Hiển thị header nhận request
springdoc.swagger-ui.showCommonExtensions=true
# Đường dẫn API docs
springdoc.api-docs.path=/v3/api-docs
# Media type mặc định
springdoc.default-produces-media-type=application/json
# Media type mặc định cho đầu vào
springdoc.default-consumes-media-type=application/json



spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB